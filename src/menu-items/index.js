import dashboard from './dashboard';
import utilities from './utilities';
import support from './support';
import profile from './profile';

const menuItems = {
  items: [dashboard, utilities, support, profile]
};

export default menuItems;
